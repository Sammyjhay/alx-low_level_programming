#include "main.h"
#include <stdio.h>

/**
 * mul - a functions that multiplies two arguments
 * @a: a parameter that will be passed into the function
 * @b: a parameter that will be passed into the function
 * Return: 0 always
 */
int mul(int a, int b)
{
	return (a * b);
}
