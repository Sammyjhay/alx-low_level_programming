#include <stdio.h>

/**
 * main - The purpose of this file is to out
 * the string: with proper grammar, but the output is a piece of art
 * followed by a newline, using printf and not puts.
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
